<!-- template_version: 1.0 (2026-08-06) — the batch-2 trap-verification step, generalized.
     Slots: {TICKER} {GEO} {SCREEN_ROW} {GEO_CATALOG} {BATCH_PRIORS} -->
You are running the deep-value TRAP VERIFICATION step on one screen-surfaced name: {TICKER} ({GEO}).
This is NOT full DD — it is the screen-to-reality check that decides whether the name advances to a court.

SCREEN ROW (treat every metric as a CLAIM to verify, not a fact — screens have documented defect
classes; two of the last three batches' rejections were OUR artifacts):
{SCREEN_ROW}

VERIFY against the geo trap catalog, every dated claim with a PRIMARY citation or an explicit
UNVERIFIED (never guess):
{GEO_CATALOG}

BATCH PRIORS (lessons already paid for — apply them, and REFUTE this briefing where it is wrong;
corrections to the prompt are first-class output):
{BATCH_PRIORS}

Universal checks regardless of geo: (1) cycle position of the screen year; (2) FCF quality —
working-capital swings, one-offs, lease/contract-liability distortions, capitalized costs;
(3) balance-sheet reality — whose cash is it (customer advances, NCI, group conduits, CB paper)?
(4) no-actor law — who can force value out, and have they CERTIFIED passivity or already spent
their catalyst? (5) filer-quality tie-out — screen EBIT vs the company's reported figure;
(6) integrity sweep per geo norms; (7) PFIC/withholding for a US-taxable holder; (8) liquidity
honestly (ADV vs a $5-10k starter); (9) live price + move since the screen date.

External data discipline: no datum from any sub-source enters your verdict without its own
claim→authority→finding line (a batch-2 verifier imported an unreported sub-agent's numbers and
had to retract them — encode the lesson).

THE FAIR-CARRY RULE (do not screen out what the desk exists to find): "fairly priced by my
computation, honest filer, tails bounded, no catalyst" is NOT a decline in an ORPHANED name —
it is the **FAIR-CARRY** verdict, a first-class outcome. In unresearched paper, the fair-value
computation itself is the edge: nobody else has done it, and the desk's fair-value estimates in
orphans have a documented conservative bias (two compounding haircuts, ~13% low; III and HSBK
both re-rated through "fair"). Before ruling a name "priced in", ask PRICED BY WHOM — check
analyst coverage / research attention; an uncovered name cannot be efficiently priced by
definition. Reserve DECLINE for names where the risk is NOT fairly paid (traps, melts,
integrity, structure) — never for "fair price, no edge" on an orphan with bounded tails.

OUTPUT (raw text, 250-400 words), exactly this shape:
"### {TICKER} (<business>) — screen <metrics> — **<VERDICT>**"
where VERDICT ∈ REAL CANDIDATE / FAIR-CARRY / BORDERLINE (lean + resolves-on) / MELTING /
PERPETUAL-TRAP / SCREEN-ARTIFACT / SCREEN-MISCLASSIFIED / INTEGRITY-KILL.
Then KILL FACTS and LIVE FACTS as cited bullets, then "RESOLVES ON:" with DATED gates, then
"Disposition:" ∈ {advance-to-court | WATCH + tripwire | AVOID/DECLINE + reopen-condition}.
FAIR-CARRY advances to court like REAL — the court adjudicates the carry bar (fairness VERIFIED,
tails bounded/unlevered, sleeve-capped, carry-vs-edge labeled), not a price gate.
If a screen metric is wrong, state the corrected value AND the defect class so it can be filed.
